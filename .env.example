# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=
CONVEX_SITE-URL=

# Clerk
VITE_CLERK_PUBLISHABLE_KEY=
CLERK_JWT_ISSUER_DOMAIN=

# Convex server-side environment variables
# These are set via `npx convex env set VAR_NAME "value"` (not in .env files)
# CLERK_WEBHOOK_SECRET=        # Svix signing secret for Clerk webhook verification
# ADMIN_EMAIL=                 # Admin user email for internal admin queries
